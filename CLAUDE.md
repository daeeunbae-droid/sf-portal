# sf-portal — 작업 규칙

SF English 학습 포털. **공개(public) 저장소**의 GitHub Pages 정적 사이트 + Firebase Realtime Database.
`index.html`이 로그인·허브이고, 거기서 Voca(`SF_Word_Test_NN.html`)와 Structure(`SF-Structure-Trainer.html`)로 보낸다.

## 세션 시작 — 이 순서로

1. **`python3 _tools/state/state.py`** — 먼저 돌린다. FAIL·WARN 이면 **그것부터 닫는다.** 새 작업을 얹지 않는다.
   *(Windows 는 `python`, Cowork Linux VM 은 `python3`. 없으면 `winget install Python.Python.3.12` —
   표준 라이브러리만 쓰므로 설치 외에 준비할 것은 없다. 파이썬이 아예 안 되면 Node 로 포팅 가능하다.)*
   이 검사기는 **기계적으로 관측 가능한 일부 미종료 신호만** 잡는다 —
   STATE 가 코드보다 뒤처졌는가, 커밋 안 된 작업이 있는가, 선언한 값이 실물과 같은가.
   **결정로그·작업대장 누락은 못 본다.** PASS 가 "제대로 닫혔다"는 뜻이 아니다.
2. **`STATE.md`** — 현재상태 캐시. 진실이 아니라 **실물을 가리키는 파생물**이다.
3. **`git log --oneline -10`**
4. STATE.md 「더 읽어야 할 때」표가 가리키는 문서를, 필요한 범위만.

`_docs/`(거버넌스 원본)는 `.gitignore`라 이 저장소에 없다. 대은의 로컬에만 있고 크다(30만 자 이상).
**기본 진입에서는 열지 않는다.** 다만 **금지가 아니다** — 실물과 STATE가 어긋날 때, 근거가 부족할 때,
과거 결정 이유를 확인할 때는 원문을 검색해도 된다. 찾은 문서는 STATE 표에 추가한다.
→ 어기면: STATE의 누락이 곧 영구적 비가시성이 된다. 안 적힌 정보는 틀린 정보보다 찾기 어렵다.

## 진실 표면 — "실물"은 단수가 아니다

| 표면 | 무엇이 사실인가 |
|---|---|
| 코드 | `git`. 문서와 어긋나면 git이 사실이다. |
| 원격 | GitHub. `origin/main` 은 **마지막 fetch 시점의 사본**이지 진짜 원격이 아니다. 확인하려면 `git fetch` 부터. |
| 배포 | GitHub Pages. **커밋됐다고 배포된 게 아니다.** 라이브 URL을 실제로 연다. |
| 학생 데이터 | Firebase. `verify.js`는 이걸 `memStore`로 떼고 돌리므로 **검증 범위 밖**이다. 실기기로만 확인된다. |

→ 어기면: "코드엔 고쳐져 있는데 학생 화면은 그대로", "로컬 PASS인데 실데이터에서 실패"가 난다. 둘 다 실제로 일어난 적 있다.

## 절대 원칙

- **실물 우선.** 문서에 없다는 이유로 "안 했다"고 판정하지 않는다. 코드와 git을 먼저 본다.
  → 어기면: 이미 끝난 일을 다시 하고, 안 끝난 일을 끝난 걸로 넘긴다.
- **화면 확인 의무.** 학생이 보는 화면을 만들었으면 실제로 열어서 눈으로 본다.
  → 어기면: 뭉개진 글자·깨진 레이아웃이 학생에게 그대로 나간다.
- **한 바퀴에 하나만.** 도중에 발견한 다른 문제는 **고치지 말고 STATE 미결에 적기만** 한다.
  → 어기면: 무엇 때문에 깨졌는지 못 찾는다.
- **`.nojekyll`을 지우지 않는다.** 밑줄 디렉터리(`_tools/`) 때문에 Pages 빌드가 통째로 멈춘다. 실제로 죽은 적 있다(`989f400`).
  → 어기면: push는 성공하는데 라이브가 안 바뀐다.
- **파일 복제로 버전 관리하지 않는다.** 단 `SF_Word_Test_NN.html` 번호 관례는 예외 — 새 번호를 라이브에 먼저 올려 실기기 확인 후 `TOOL_URL.voca` 한 줄만 옮긴다. 그 한 줄이 롤백 스위치다 [#SFP-012-6].
  → 어기면: 롤백이 한 줄에서 복구 작업으로 바뀐다.
- **이 저장소는 공개다.** 커밋 전에 "공개돼도 되는가"를 묻는다. 시중 교재 원본·문항 정답·출판사 상품명·비밀번호는 커밋하지 않는다.
  → 어기면: 판매 예정 자산이 인터넷에 공개된다.

## 대은 승인 없이 코드로 정하지 않는다

Firebase 데이터 스키마 변경(경로 신설·이름 변경 포함) · 학생·학부모에게 보이는 문구 · 교육 내용·난이도·문항 구성 · 도구 간 공통 구조 신설

## 검증

```bash
cd _tools/sfp012 && npm install && node verify.js     # 기준선 59 PASS / 0 FAIL
```

- **`SF_Word_Test_19.html`을 지우면 안 된다.** 이 스크립트가 19를 "정답지"로 놓고 20과 대조한다.
- **이 검사기는 아직 gate가 아니다.** 실행하라고 여기 적혀 있을 뿐이라, 급하면 건너뛰어진다.
  pre-commit hook 이나 배포 스크립트에 묶는 것이 STATE 미결 E다. 그 전까지는 **사람이 지켜야 하는 문장**임을 알고 쓴다.
- 검증을 통과하면 **화면 확인 전에 일단 커밋**한다. 에러로 멈추면 작업이 사라진다.

## STATE.md 를 쓸 때

- **현재상태 캐시이지 요약이 아니다.** 판단·근거·설계안은 결정로그·작업대장의 몫이고, 여기엔 **결정 ID**만 붙인다 (`[#SFP-013]`).
  전 항목에 출처·검증일을 달지 않는다 — 그러면 메타데이터로 다시 부푼다.
- 주장은 세 종류로만 구분한다. ① 검사기가 대조하는 값 ② 결정에서 온 값(`[#SFP-NNN]`) ③ **미확인**.
- **현재 상태는 덮어쓴다. 역사는 쌓지 않는다.** 이유는 결정로그, 사건은 작업대장.
- **HEAD 해시를 적지 않는다.** STATE 는 git 에 추적되므로 자기 자신이 든 HEAD 를 가질 수 없다 —
  적으면 「적는다 → 커밋한다 → HEAD 가 바뀐다 → 틀린다」가 끝없이 돈다.
  신선도는 검사기가 **git 이력**(STATE 마지막 커밋 vs 감시 대상 마지막 커밋)에서 계산한다.
  → 코드와 STATE 를 같은 커밋에 넣어도, 코드 뒤에 STATE 마감 커밋을 따로 만들어도 통과한다.
- `state-check` 블록을 고쳤으면 **`state.py` PASS 를 확인하고** 커밋한다.

## 세션 종료 — 이 순서로. 넷 다 되어야 한 바퀴가 닫힌다

1. **기록** — 결정이 났으면 결정로그에, 코드가 바뀌었으면 작업대장에 등재한다.
   **아무것도 없었으면 아무 데도 적지 않는다** — "결정 없음" 레코드를 쌓지 않는다(로그가 다시 부푼다).
2. **`STATE.md` 갱신** + 다음 바퀴의 Active 지정(≤1).
3. **커밋** — 코드와 STATE 를 한 커밋에 넣어도 되고, 코드 뒤에 STATE 마감 커밋을 따로 만들어도 된다.
   검사기는 둘 다 허용한다. **push 는 종료 조건이 아니다**(자격증명 없는 환경도 있다).
   미push 는 알림일 뿐 막지 않으며, 밀린 push 는 STATE 미결로 관리한다.
4. **`state.py` PASS 확인** — 검사는 커밋 **뒤**다. STATE 를 고치면 tree 가 dirty 해져 그 전에는 WARN 이 뜬다.

하나라도 빠지면 바퀴가 안 닫힌 것이다.
**다음 세션의 `state.py`는 그중 일부만 잡는다** — STATE 뒤처짐과 미커밋은 잡지만,
**결정로그·작업대장 누락은 못 잡는다.** 그건 사람이 확인해야 한다.
(SFP-011·012가 1~2번에서 끊겼고, 당시엔 잡아줄 장치가 아무것도 없어 SFP-013 세션을 통째로 복구에 썼다.)
